{
  "mcpServers": {
    "datadog": {
      "url": "https://${DD_MCP_DOMAIN:-mcp.datadoghq.com}/v1/mcp?referrer_ide=cursor-plugin&plugin_version=0.7.15&toolsets=${DD_MCP_TOOLSETS:-core,visualizations}",
      "headers": {
        "DD_API_KEY": "${env:DD_API_KEY}",
        "DD_APPLICATION_KEY": "${env:DD_APPLICATION_KEY}"
      }
    }
  }
}
